class Board{
	constructor(rows, columns,  player){
		rows: 10,
		columns: 16,
		user: this.user
	}
	fillBoard () {
		
	}
}

function getUsers(){
return fetch(http://localhost:3000/users)
.then(res => res.json())
.then(console.log(json))
